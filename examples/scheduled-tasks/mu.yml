---

environments:
  - name: acceptance
    cluster:
      maxSize: 1
  - name: production
    cluster:
      maxSize: 1

service:
  name: issue-158-scheduled-tasks
  healthEndpoint: /
  port: 80
  pathPatterns:
    - /*
  schedules:
    - expression: '* * * * *'
      command: echo updating bananas
  pipeline:
    source:
      repo: timbaileyjones/banana-service
