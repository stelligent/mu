---

environments:
  - name: acceptance
    cluster:
      maxSize: 1
      keyName: timbaileyjones-labs-keypair
  - name: production
    cluster:
      maxSize: 1
      keyName: timbaileyjones-labs-keypair

service:
  name: issue-158
  healthEndpoint: /
  port: 80
  pathPatterns:
    - /*
  schedules:
    - expression: cron(* * * * * *)
      command: echo updating bananas
  pipeline:
    source:
      provider: GitHub
      repo: timbaileyjones/banana-service
      branch: master
