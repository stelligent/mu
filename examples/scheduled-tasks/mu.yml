---

environments:
  - name: acceptance
    cluster:
      maxSize: 1
  - name: production
    cluster:
      maxSize: 1

service:
  name: issue-158
  healthEndpoint: /
  port: 80
  pathPatterns:
    - /*
  schedules:
    - expression: cron(* * * * * *)
      command: echo updating bananas
  pipeline:
    source:
      provider: GitHub
      repo: timbaileyjones/banana-service
      branch: master
