---

environments:
  - name: ec2-dev
    provider: ec2
    cluster:
      keyName: mu-key
  - name: ecs-dev
    provider: ecs
    cluster:
      keyName: mu-key

service:
  name: basic-example
  healthEndpoint: /
  port: 8000
  pathPatterns:
    - /*

